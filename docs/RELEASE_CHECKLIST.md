# 发布验收记录

验收日期：2026-07-16

## 数据与导入

- [x] 数据只包含已发布、未删除的网址工具；
- [x] 69 个栏目定义；
- [x] 3,229 条工具和 3,229 条产品内容；
- [x] 297 条多分类关系；
- [x] 16,527 条标签关系；
- [x] 3,050 个按处理后内容 SHA-256 去重的媒体文件；
- [x] 临时空库真实执行导入成功；
- [x] 模拟新装站时自动增加 7 个产品扩展字段；
- [x] 临时目标系统配置表保持空，没有导入源站配置；
- [x] 临时数据库和测试文件在验证后已清理。

## 模板与脚本

- [x] 模板静态资源引用完整；
- [x] 模板 include 引用完整；
- [x] 4 个 PHP CLI 脚本通过 PHP 7.4 语法检查；
- [x] 4 个 PHP 脚本均限制为 CLI 执行，误放到 Web 目录时返回 404；
- [x] 导入器默认 dry-run；
- [x] 导入器对已存在栏目同时比较 slug、父栏目和频道类型；对已存在工具比较标题、频道、主分类、产品正文记录和官网 identity；
- [x] 导入器自身拒绝私网/带凭据网址、危险 HTML，以及目标目录中内容不同的同名媒体；
- [x] 排行管理脚本默认 dry-run；
- [x] 首页返回 HTTP 200；
- [x] `/ranking/` 返回 HTTP 200；
- [x] `/search.html?keywords=AI&method=1` 返回 HTTP 200；
- [x] 页面响应未发现 PHP Fatal、Parse、模板缺失或数据库错误标记。

## 编码与隐私

- [x] 项目编码守卫 P0/P1/P2 均为 0；
- [x] 发布审计器检查文本 UTF-8、Token、私钥、数据库凭据和 JSON 私密字段；
- [x] 发布审计器通过；
- [x] 源项目绝对路径残留为 0；
- [x] 源数据库名称残留为 0；
- [x] 源站域名残留为 0；
- [x] 源站可见品牌文案残留为 0，旧品牌图片已替换为通用 SVG；
- [x] canonical/schema 只信任后台 `web_basehost`，不再直接使用请求 Host 头；
- [x] 已识别并移除许可证禁止用于 Open Source 的 Fancybox 6.1.14，包括库文件、API 调用和专用样式；
- [x] 详情图已迁移到 MIT 许可的 PhotoSwipe 5.4.4，上游 LICENSE 随包保留；
- [x] 图片已去除元数据；
- [x] JSON 清洗了 38 处邮箱和 7 处手机号；
- [x] JSON 未发现已知 Token 或私钥块。

当前数据文件 SHA-256：

```text
19E25AF1B64C86821D7282784FDB28DBA09E707A80A518FABED68A5A5D393D0C
```

## 非阻塞数据提醒

- 5 条官网仍为 HTTP；
- 9 组同名记录；
- 3 组相同官网 host/path；
- 上述 17 项由发布审计器输出 warning，详见 `docs/DATA_QUALITY.md`，未做可能误伤数据的自动合并。

## 独立 Git 暂存模拟

- 3,466 个将被纳入的文件，总计约 48.34 MiB；
- 最大文件为 `data/navigation-content.v1.json`，约 16.12 MiB；
- 0 个零字节文件，0 个超过 50 MiB 的文件；
- 0 个数据库、配置、备份、私钥、日志或压缩包类禁止文件；
- `git diff --cached --check` 为 0 个问题；
- `scripts/pre_publish_check.ps1` 已在 Windows PowerShell 5 的独立临时 Git 环境中实际通过；
- 当前电脑未安装 Gitleaks，内置扫描已通过，但正式公开前仍建议补做一次 Gitleaks 全历史扫描。

## 发布者仍需人工确认

- [ ] 模板来源、`iowen-iconfont.woff2`、第三方网站 Logo/图片和较长描述的再分发权；这是公开发布阻塞项；
- [ ] README 中的仓库名称、截图和维护者信息；
- [ ] Git 暂存区与完整历史的 Gitleaks 扫描；
- [ ] 在实际准备公开的易优 CMS 版本上再走一次安装流程；
- [ ] 不把目标站配置、备份、Session、日志和整个 uploads 目录加入仓库。
